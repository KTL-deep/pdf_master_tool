# 📄 PDF Master Tool

**PDF Master Tool** — это модульное Python-приложение для комплексной работы с PDF-документами. Проект реализует функционал популярных онлайн-сервисов для работы с PDF, но выполняется локально, обеспечивая конфиденциальность данных и отсутствие ограничений по размеру файлов.

## 🚀 Возможности

Архитектура проекта разбита на 6 логических модулей:

### 1. Организация (Organize) ✅ *В разработке*
* [x] **Объединение PDF:** Слияние нескольких файлов в один.
* [x] **Разделение PDF:** Разбивка файла на отдельные страницы.
* [x] **Управление страницами:** Удаление и перестановка страниц.
* [x] **Извлечение:** Сохранение встроенных изображений из PDF.

### 2. Оптимизация (Optimize)
* **Сжатие:** Уменьшение размера файла.
* **OCR:** Распознавание текста (Tesseract).
* **Восстановление:** Исправление поврежденных PDF.

### 3. Конвертация (Convert)
* **В PDF:** Из изображений, Word, Excel, HTML.
* **Из PDF:** В JPG, Word, Excel, PDF/A.

### 4. Редактирование (Edit)
* Поворот страниц.
* Добавление номеров страниц.
* Наложение водяных знаков.

### 5. Защита (Security)
* Установка пароля (шифрование).
* Снятие защиты.
* Цифровая подпись.

---

## 🛠 Установка и Настройка

### Предварительные требования
* Python 3.10+ (рекомендуется 3.12/3.13)
* Windows / macOS / Linux

### Шаги установки

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/your-username/pdf_master_tool.git](https://github.com/your-username/pdf_master_tool.git)
    cd pdf_master_tool
    ```

2.  **Создайте виртуальное окружение (рекомендуется):**
    ```bash
    python -m venv venv
    # Windows:
    venv\Scripts\activate
    # Mac/Linux:
    source venv/bin/activate
    ```

3.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

4.  **Дополнительные системные требования (для будущих модулей):**
    * Для работы модуля OCR требуется установленный **Tesseract**.
    * Для конвертации `pdf2image` требуется установленный **Poppler**.

---

## 📂 Структура проекта

Проект построен на архитектуре, разделяющей логику (Core) и представление (Interface).

```text
pdf_master_tool/
├── data/                 # Временные файлы и примеры для тестов
├── src/
│   ├── core/             # Бизнес-логика (Back-end)
│   │   ├── organize.py   # Слияние, разделение, структура
│   │   ├── optimize.py   # Сжатие и OCR
│   │   ├── convert_*.py  # Конвертеры
│   │   ├── edit.py       # Редактирование страниц
│   │   └── security.py   # Шифрование и защита
│   ├── interface/        # Графический интерфейс (Front-end)
│   └── utils/            # Логгеры и хелперы
├── tests/                # Unit-тесты
├── main.py               # Точка входа
└── requirements.txt
